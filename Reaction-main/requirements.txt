python-telegram-bot[ext]==21.6
pyrogram==2.0.106
tgcrypto==1.2.5
pymongo==4.6.3
motor==3.3.2
# Explicitly include a Motor-supported PyMongo build to avoid import mismatches
# (e.g., `_QUERY_OPTIONS` removal in PyMongo 4.10+).
apscheduler==3.10.4
psutil==6.1.0
